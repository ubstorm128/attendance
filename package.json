{
  "scripts": {
    "build": "tsc",
    "start": "npm run build && node server.js",
    "dev": "npm start",
    "dev:watch": "tsc --watch"
  },
  "devDependencies": {
    "@types/node": "^26.1.2",
    "typescript": "^7.0.2"
  }
}